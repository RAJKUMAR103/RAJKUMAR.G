#include<stdio.h>
#include<string.h>
void main()
{
    int a;
    printf("enter a values:");
    scanf("%d",&a);
    if(a%2==0)
    printf("it is a prime no");
    else
    printf("it is not a prime");
}
